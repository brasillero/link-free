{
  "name": "link-free",
  "version": "0.1.0",
  "description": "Tiny link-in-bio static HTML generator - semantic, SEO-first, zero JS",
  "type": "module",
  "license": "MIT",
  "author": "brasillero",
  "repository": {
    "type": "git",
    "url": "https://github.com/brasillero/link-free.git"
  },
  "homepage": "https://github.com/brasillero/link-free",
  "keywords": [
    "link-in-bio",
    "linktree-alternative",
    "static-site",
    "seo",
    "cli"
  ],
  "bin": {
    "link-free": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "css": "node scripts/build-styles.mjs",
    "build": "pnpm css && tsup",
    "typecheck": "pnpm css && tsc --noEmit",
    "test": "pnpm css && vitest run",
    "dev": "pnpm css && vitest",
    "schemas": "vite-node scripts/generate-schemas.ts",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@types/node": "^20.14.0",
    "tailwindcss": "^4.3.3",
    "tsup": "^8.1.0",
    "typescript": "^5.5.4",
    "vite-node": "^1.6.1",
    "vitest": "^1.6.0",
    "zod": "^3.25.76",
    "zod-to-json-schema": "^3.25.2"
  },
  "dependencies": {
    "jiti": "^2.7.0"
  }
}
